
export type ButtonProps = {
    onClick: Function;
    text: string;
  };

  
export type ErrorProps = {
  message: string;
};

export type LabelProps = {
  text: string;
  htmlFor: string;
};

export type AvatarProps = {
  picture: string;
};